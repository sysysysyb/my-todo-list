import styled from "@emotion/styled";

const Container = styled.div`
  background-color: #ffffff;
  border-radius: 0.3rem;
  flex: 1;
`;

function StopWatch() {
  return <Container>StopWatch</Container>;
}

export default StopWatch;
